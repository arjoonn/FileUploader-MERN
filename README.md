It is a basic MERN stack project for uploading files.
It uses multer module of express to store files in  the DB.
it can be used a  storage space for the user.It contains a view section where users can view the uploaded files by typing the file name.
